[project]
name = "llm-engineering-in-100-lines"
version = "0.1.0"
description = "19 self-contained Python POCs exploring LLM engineering from first API call to MCP protocols"
requires-python = ">=3.10"
dependencies = [
    "requests==2.34.2",
    "ollama==0.6.2",
    "colorama==0.4.6",
]

[project.optional-dependencies]
dev = ["ruff"]
